# RSA Encryptor Service - Документация

## 📋 Содержание
1. [Обзор](#обзор)
2. [Архитектура](#архитектура)
3. [Функционал](#функционал)
4. [Установка и запуск](#установка-и-запуск)
5. [Использование](#использование)
6. [API Документация](#api-документация)
7. [Информационная безопасность](#информационная-безопасность)
8. [Интеграция с CI/CD](#интеграция-с-cicd)
9. [Мониторинг и логирование](#мониторинг-и-логирование)
10. [Вопросы и ответы](#вопросы-и-ответы)

## 🎯 Обзор

### Цель сервиса
**RSA Encryptor Service** — веб-сервис для безопасного шифрования данных с использованием асимметричной криптографии RSA 4096-bit. Сервис предназначен для сценариев, где необходимо публично передавать зашифрованные данные, которые будут расшифровываться в защищенной среде автоматизации.

### Ключевые особенности
- 🔐 **Только шифрование** — приватный ключ никогда не покидает защищенную среду
- 🌐 **Веб-интерфейс и API** — двойной интерфейс для удобства использования
- 🚀 **Готовые примеры** — код для расшифровки на популярных языках
- 📊 **Логирование** — аудит всех операций
- 🔧 **Простота развертывания** — Docker, бинарные файлы, исходный код

### Целевые сценарии использования
1. Передача секретов через публичные каналы (SD, чаты, системы управления задачами)
2. Шифрование конфигурационных данных для CI/CD пайплайнов
3. Защищенный обмен данными между различными средами
4. Хранение зашифрованных секретов в системах контроля версий

## 🏗️ Архитектура

### Технологический стек
```
Backend:    Go 1.21+
Frontend:   HTML5, CSS3, JavaScript
Шифрование: RSA 4096-bit (OAEP-SHA256)
Формат:     PEM → Base64
```

### Диаграмма архитектуры
```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│    Клиент       │────▶│   RSA Encryptor │────▶│   Защищенная    │
│   (Публичный    │     │     Service     │     │    Среда        │
│    канал)       │◀────│   (public.pem)  │◀────│   (private.pem) │
└─────────────────┘     └─────────────────┘     └─────────────────┘
        │                        │                        │
        ▼                        ▼                        ▼
   Открытый текст          Зашифрованный           Расшифрованный
                         текст (Base64)              текст
```

### Компоненты
1. **Веб-сервер** — обработка HTTP запросов (Go net/http)
2. **Криптографический модуль** — RSA шифрование (crypto/rsa)
3. **Шаблонизатор** — рендеринг HTML (html/template)
4. **Статические ресурсы** — CSS, JS, иконки

## ⚙️ Функционал

### Основные возможности
1. **Шифрование текста через веб-интерфейс**
   - Интуитивная форма ввода
   - Подсчет символов в реальном времени
   - Визуальная обратная связь
   - Копирование результата в один клик

2. **REST API для автоматизации**
   - JSON API для интеграции с другими системами
   - Стандартные HTTP статусы
   - Подробные сообщения об ошибках

3. **Интегрированная документация**
   - Примеры кода для расшифровки
   - Инструкции по использованию OpenSSL
   - Примеры на Python, Node.js, Bash

4. **Мониторинг и диагностика**
   - Health check эндпоинт
   - Логирование операций
   - Отображение статуса сервиса

### Лимиты и ограничения
| Параметр | Значение | Описание |
|----------|----------|----------|
| Макс. размер текста | ~400 символов | Ограничение RSA 4096-bit |
| Алгоритм шифрования | RSA-OAEP-SHA256 | Современный стандарт |
| Формат ключа | PEM | Стандартный формат OpenSSL |
| Формат вывода | Base64 | Удобно для передачи |

## 🚀 Установка и запуск

### Предварительные требования
- Go 1.21+ или Docker
- Публичный ключ RSA 4096-bit в формате PEM

### Создание ключей
```bash
# Генерация пары RSA ключей
openssl genrsa -out private.pem 4096
openssl rsa -in private.pem -pubout -out public.pem
```

### Способ 1: Запуск из исходного кода
```bash
# Клонирование и настройка
git clone <repository-url>
cd rsa-encryptor-service

# Установка зависимостей
go mod download

# Запуск сервиса
export RSA_PUBLIC_KEY_PATH=./public.pem
export PORT=8080
go run main.go
```

### Способ 2: Docker
```bash
# Сборка образа
docker build -t rsa-encryptor .

# Запуск контейнера
docker run -p 8080:8080 \
  -v $(pwd)/public.pem:/app/public.pem:ro \
  -e RSA_PUBLIC_KEY_PATH=/app/public.pem \
  rsa-encryptor
```

### Способ 3: Docker Compose
```yaml
# docker-compose.yml
version: '3.8'
services:
  rsa-encryptor:
    build: .
    ports:
      - "8080:8080"
    environment:
      - RSA_PUBLIC_KEY_PATH=/app/public.pem
      - PORT=8080
    volumes:
      - ./public.pem:/app/public.pem:ro
    restart: unless-stopped
```

```bash
docker-compose up -d
```

### Проверка работоспособности
```bash
# Проверка health check
curl http://localhost:8080/health

# Проверка веб-интерфейса
open http://localhost:8080
```

## 🖥️ Использование

### Веб-интерфейс
1. Откройте `http://localhost:8080` в браузере
2. Введите текст для шифрования в текстовое поле
3. Нажмите "Зашифровать"
4. Скопируйте Base64 строку из результата

### Командная строка
```bash
# Пример шифрования через cURL
curl -X POST http://localhost:8080/encrypt \
  -H "Content-Type: application/json" \
  -d '{"text": "API_SECRET_KEY_123"}'

# Ответ:
{
  "encrypted": "MIIF...OOQ==",
  "original": "API_SECRET_KEY_123",
  "format": "base64",
  "algorithm": "RSA-OAEP-SHA256"
}
```

### Расшифровка данных
```bash
# С помощью OpenSSL
echo "MIIF...OOQ==" | base64 -d | \
  openssl pkeyutl -decrypt \
    -inkey private.pem \
    -pkeyopt rsa_padding_mode:oaep \
    -pkeyopt rsa_oaep_md:sha256
```

## 📡 API Документация

### Эндпоинты

#### `GET /`
**Описание**: Веб-интерфейс сервиса  
**Ответ**: HTML страница

#### `POST /encrypt`
**Описание**: Шифрование текста  
**Content-Type**: `application/json` или `application/x-www-form-urlencoded`

**Параметры**:
```json
{
  "text": "string"  // Текст для шифрования
}
```

**Успешный ответ (200 OK)**:
```json
{
  "encrypted": "string",    // Base64 зашифрованный текст
  "original": "string",     // Оригинальный текст
  "format": "base64",       // Формат вывода
  "algorithm": "RSA-OAEP-SHA256"  // Используемый алгоритм
}
```

**Ошибки**:
- `400 Bad Request` — неверный формат запроса
- `500 Internal Server Error` — ошибка шифрования

#### `GET /health`
**Описание**: Проверка работоспособности сервиса  
**Ответ**:
```json
{
  "status": "ok",
  "service": "rsa-encryptor",
  "key_loaded": true,
  "key_size": 4096
}
```

### Примеры использования API

#### Python
```python
import requests
import json

def encrypt_text(text, service_url="http://localhost:8080"):
    response = requests.post(
        f"{service_url}/encrypt",
        headers={"Content-Type": "application/json"},
        data=json.dumps({"text": text})
    )
    if response.status_code == 200:
        return response.json()["encrypted"]
    else:
        raise Exception(f"Ошибка: {response.text}")

# Использование
encrypted = encrypt_text("SECRET_API_KEY")
print(f"Зашифровано: {encrypted}")
```

#### Node.js
```javascript
const fetch = require('node-fetch');

async function encryptText(text, serviceUrl = 'http://localhost:8080') {
    const response = await fetch(`${serviceUrl}/encrypt`, {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({ text })
    });
    
    if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${await response.text()}`);
    }
    
    return (await response.json()).encrypted;
}

// Использование
encryptText('SECRET_DATA')
    .then(encrypted => console.log('Зашифровано:', encrypted))
    .catch(err => console.error('Ошибка:', err));
```

#### Bash
```bash
#!/bin/bash

ENCRYPTOR_URL="http://localhost:8080"

encrypt_string() {
    local text="$1"
    curl -s -X POST "$ENCRYPTOR_URL/encrypt" \
        -H "Content-Type: application/json" \
        -d "{\"text\": \"$text\"}" | \
        python3 -c "import sys, json; print(json.load(sys.stdin)['encrypted'])"
}

# Использование
SECRET="production_database_password"
ENCRYPTED=$(encrypt_string "$SECRET")
echo "Зашифрованный пароль: $ENCRYPTED"
```

## 🔒 Информационная безопасность

### Принципы безопасности

#### 1. **Минимализм и простота**
- Только необходимый функционал
- Никаких сложных зависимостей
- Минимальная поверхность для атак

#### 2. **Разделение ответственности**
```
Публичный сервис              Защищенная среда
├── Шифрование                ├── Расшифровка
├── Публичный ключ            ├── Приватный ключ
└── Нет доступа к данным      └── Полный контроль над данными
```

#### 3. **Неизменяемость ключей**
- Ключи загружаются только при старте
- Никакой динамической генерации
- Невозможность перезаписи ключей

### Соответствие требованиям ИБ

#### ✅ **Конфиденциальность**
| Требование | Реализация | Соответствие |
|------------|------------|--------------|
| Защита данных при передаче | HTTPS (рекомендуется) | ✅ |
| Шифрование секретов | RSA 4096-bit | ✅ |
| Отсутствие утечек ключей | Приватный ключ только в защищенной среде | ✅ |
| Безопасное хранение ключей | Файловая система с ограниченным доступом | ✅ |

#### ✅ **Целостность**
| Требование | Реализация | Соответствие |
|------------|------------|--------------|
| Проверка целостности данных | Base64 валидация | ✅ |
| Защита от подмены данных | RSA обеспечивает целостность | ✅ |
| Аудит операций | Логирование всех запросов | ✅ |

#### ✅ **Доступность**
| Требование | Реализация | Соответствие |
|------------|------------|--------------|
| Высокая доступность | Легковесный сервис, быстрое восстановление | ✅ |
| Мониторинг работоспособности | Health check эндпоинт | ✅ |
| Защита от DDoS | Rate limiting (рекомендуется) | ⚠️ |

#### ✅ **Аудит и отслеживаемость**
| Требование | Реализация | Соответствие |
|------------|------------|--------------|
| Логирование операций | Подробные логи | ✅ |
| Трассировка запросов | IP адреса, временные метки | ✅ |
| Анализ инцидентов | Структурированные логи | ✅ |

### Рекомендации по безопасности

#### Настройка сервера
```nginx
# Пример конфигурации Nginx для HTTPS
server {
    listen 443 ssl;
    server_name encryptor.example.com;
    
    ssl_certificate /etc/ssl/certs/encryptor.crt;
    ssl_certificate_key /etc/ssl/private/encryptor.key;
    
    # Усиленные настройки безопасности
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    
    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### Права доступа
```bash
# Настройка прав доступа к ключам
chmod 600 private.pem      # Только владелец может читать/писать
chmod 644 public.pem       # Все могут читать

# Настройка пользователя для сервиса
useradd --system --shell /bin/false rsa-encryptor
chown -R rsa-encryptor:rsa-encryptor /opt/rsa-encryptor
```

#### Ротация ключей
```bash
#!/bin/bash
# Скрипт ротации ключей

# Генерация новых ключей
openssl genrsa -out private_new.pem 4096
openssl rsa -in private_new.pem -pubout -out public_new.pem

# Перезапуск сервиса с новым публичным ключом
cp public_new.pem /opt/rsa-encryptor/public.pem
systemctl restart rsa-encryptor

# Миграция данных (если необходимо)
# ...
```

## 🔄 Интеграция с CI/CD

### GitLab CI
```yaml
# .gitlab-ci.yml
variables:
  ENCRYPTOR_URL: "https://encryptor.example.com"

stages:
  - encrypt-secrets
  - deploy

encrypt-secrets:
  stage: encrypt-secrets
  script:
    # Шифрование секретов
    - ENCRYPTED_DB_PASS=$(curl -s -X POST "$ENCRYPTOR_URL/encrypt" \
        -H "Content-Type: application/json" \
        -d "{\"text\": \"$DB_PASSWORD\"}" | jq -r '.encrypted')
    
    # Сохранение в переменные GitLab
    - curl --request PUT --header "PRIVATE-TOKEN: $CI_JOB_TOKEN" \
        "$CI_API_V4_URL/projects/$CI_PROJECT_ID/variables/ENCRYPTED_DB_PASSWORD" \
        --form "value=$ENCRYPTED_DB_PASS" \
        --form "protected=true" \
        --form "masked=true"
  only:
    - main
```

### GitHub Actions
```yaml
# .github/workflows/deploy.yml
name: Deploy with encrypted secrets

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Encrypt secrets
      env:
        ENCRYPTOR_URL: "https://encryptor.example.com"
      run: |
        ENCRYPTED_API_KEY=$(curl -s -X POST "$ENCRYPTOR_URL/encrypt" \
          -H "Content-Type: application/json" \
          -d "{\"text\": \"${{ secrets.API_KEY }}\"}" | \
          jq -r '.encrypted')
        
        echo "ENCRYPTED_API_KEY=$ENCRYPTED_API_KEY" >> $GITHUB_ENV
    
    - name: Deploy
      run: |
        # Расшифровка и использование на стороне сервера
        echo "ENCRYPTED_API_KEY=$ENCRYPTED_API_KEY" >> deploy.env
```

### Jenkins Pipeline
```groovy
// Jenkinsfile
pipeline {
    agent any
    
    environment {
        ENCRYPTOR_URL = 'https://encryptor.example.com'
    }
    
    stages {
        stage('Encrypt Secrets') {
            steps {
                script {
                    // Шифрование секретов
                    def encrypted = sh(
                        script: """
                            curl -s -X POST "$ENCRYPTOR_URL/encrypt" \
                                -H "Content-Type: application/json" \
                                -d '{"text": "${DB_PASSWORD}"}' | \
                                jq -r '.encrypted'
                        """,
                        returnStdout: true
                    ).trim()
                    
                    env.ENCRYPTED_DB_PASSWORD = encrypted
                }
            }
        }
        
        stage('Deploy') {
            steps {
                // Передача зашифрованных данных
                sh """
                    echo "DB_PASSWORD_ENCRYPTED=$ENCRYPTED_DB_PASSWORD" > encrypted_secrets.txt
                    scp encrypted_secrets.txt deploy@server:/tmp/
                """
            }
        }
    }
}
```

## 📊 Мониторинг и логирование

### Конфигурация логов
```bash
# Структура логов
/var/log/rsa-encryptor/
├── access.log     # Логи доступа
├── error.log      # Ошибки приложения
└── audit.log      # Аудит операций
```

### Формат логов
```json
{
  "timestamp": "2024-01-15T10:30:00Z",
  "level": "INFO",
  "method": "POST",
  "path": "/encrypt",
  "client_ip": "192.168.1.100",
  "user_agent": "curl/7.68.0",
  "request_size": 42,
  "response_size": 512,
  "processing_time_ms": 5,
  "status_code": 200,
  "encrypted_chars": 25
}
```

### Prometheus метрики
```go
// Пример метрик
var (
    requestsTotal = prometheus.NewCounterVec(
        prometheus.CounterOpts{
            Name: "http_requests_total",
            Help: "Total number of HTTP requests",
        },
        []string{"method", "endpoint", "status"},
    )
    
    encryptionTime = prometheus.NewHistogram(
        prometheus.HistogramOpts{
            Name: "encryption_time_seconds",
            Help: "Time spent encrypting data",
        },
    )
)
```

### Grafana дашборд
```
📈 Метрики для мониторинга:
1. Rate шифрований в секунду
2. 95-й перцентиль времени ответа
3. Распределение размеров запросов
4. Частота ошибок по типам
5. География запросов (по IP)
```

## ❓ Вопросы и ответы

### Часто задаваемые вопросы

#### Q: Почему только 400 символов можно зашифровать?
**A**: Это ограничение RSA 4096-bit. Для шифрования больших данных используйте гибридное шифрование (RSA + AES).

#### Q: Как защитить приватный ключ?
**A**: Рекомендуемые меры:
1. Хранить в защищенном хранилище (Hashicorp Vault, AWS KMS)
2. Использовать аппаратные модули безопасности (HSM)
3. Регулярно ротировать ключи
4. Ограничить доступ по принципу наименьших привилегий

#### Q: Что делать при компрометации ключа?
**A**: Процедура реагирования:
1. Немедленно сгенерировать новую пару ключей
2. Обновить публичный ключ в сервисе
3. Перешифровать все данные новым ключом
4. Инвалидировать старый ключ

#### Q: Как масштабировать сервис?
**A**: Рекомендации по масштабированию:
1. Использовать балансировщик нагрузки
2. Кэшировать публичный ключ в памяти
3. Настроить репликацию ключей между нодами
4. Использовать распределенное хранилище для логов

#### Q: Можно ли использовать для PCI DSS?
**A**: Сервис может быть частью решения, но требуется:
1. Аудит кода сторонней организацией
2. Использование HSM для ключей
3. Полное логирование всех операций
4. Регулярное тестирование на проникновение

### Ограничения и предупреждения

⚠️ **Важно**:
1. Сервис не заменяет полноценную систему управления секретами
2. Не использовать для шифрования паролей пользователей
3. Регулярно обновлять зависимости
4. Проводить security assessment перед использованием в продакшене

### Поддержка и сообщество
- 📚 Документация: [ссылка на документацию]
- 🐛 Баг-репорты: [ссылка на issue tracker]
- 💬 Обсуждение: [ссылка на форум/чат]
- 🔄 Обновления: [ссылка на changelog]

---

## 📄 Лицензия

```
MIT License

Copyright (c) 2025 RSA Encryptor Service

Данное программное обеспечение предоставляется "как есть", без каких-либо гарантий. 
Авторы не несут ответственности за возможный ущерб, вызванный использованием 
этого программного обеспечения.

Полный текст лицензии доступен по адресу:
https://opensource.org/licenses/MIT
```

## 🤝 Вклад в развитие

Мы приветствуем вклад в развитие проекта! Пожалуйста:
1. Создайте issue для обсуждения изменений
2. Сделайте fork репозитория
3. Создайте feature branch
4. Добавьте тесты для новых функций
5. Создайте pull request

---

*Последнее обновление: Февраль 2026*  
*Версия документации: 1.0*  
*Соответствие стандартам: NIST SP 800-57, RFC 8017*